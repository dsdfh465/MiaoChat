# 妙语记账（MiaoChat）项目开发十诫

> 即日起生效。所有任务按此执行。本文件为项目「宪法」，Cursor 规则（`.cursor/rules/`）为其可执行副本。

## 一、架构师/程序员身份定位

Cursor 在本项目中担任 **执行架构师 + 高级程序员** 双重角色：

| 身份 | 职责 |
|---|---|
| 执行架构师 | 技术选型落地、代码结构设计、数据库设计、API 设计 |
| 高级程序员 | 编码实现、单元测试、代码重构、性能优化 |

**核心原则：** 所有代码必须 **可运行、可测试、可维护、可扩展**，缺一不可。

## 二、代码规范（硬性约束）

### 2.1 TypeScript / Node.js 规范

- 使用 ES Module 语法（`import`/`export`），禁止使用 `require`/`module.exports`
- 所有函数必须声明参数类型和返回值类型，禁止使用 `any`
- 所有 API 接口必须使用 Zod 或 Joi 进行请求参数校验
- 错误处理必须使用 try-catch，所有异常需记录日志
- 敏感信息（数据库密码、API Key）必须通过 `.env` 环境变量注入，严禁硬编码
- 代码缩进使用 2 个空格

### 2.2 Flutter / Dart 规范

- 所有 Widget 必须声明为 `StatelessWidget` 或 `StatefulWidget`，禁止使用 Function 构建 UI
- 状态管理统一使用 Riverpod，禁止混用 Provider/Bloc/GetX
- 所有异步操作必须使用 `Future`/`async`/`await`，禁止回调地狱
- 颜色、文字样式、间距必须定义为常量/主题，禁止硬编码数值
- 所有 `BuildContext` 引用必须确保 mounted 检查（`if (!mounted) return;`）
- 代码缩进使用 2 个空格

### 2.3 命名规范

| 类型 | 规范 | 示例 |
|---|---|---|
| 文件名（TS/JS） | kebab-case；分层文件用 `{entity}.{layer}.ts` | `user-service.ts` / `transaction.controller.ts` |
| 文件名（Dart） | snake_case | `home_screen.dart` |
| 类名（TS/JS/Dart） | PascalCase | `UserController` |
| 函数/变量（TS/JS） | camelCase | `getUserById` |
| 函数/变量（Dart） | camelCase | `getUserById` |
| 常量 | UPPER_SNAKE_CASE | `MAX_RETRY_COUNT` |
| 数据库表名 | snake_case | `user_transactions` |
| API 路径 | kebab-case | `/api/user-transactions` |

> 后端分层文件以目录结构示例为准：`transaction.controller.ts`、`transaction.service.ts`。

## 三、文档规范

### 3.1 代码注释

- 每个 `.ts`/`.dart` 文件顶部必须包含文件头注释（说明文件用途）
- 每个公开函数/方法必须包含 JSDoc 风格注释，说明功能、参数、返回值
- 复杂业务逻辑必须添加行内注释说明意图
- 禁止注释掉的死代码（Dead Code），一律删除

文件头示例（TS）：

```typescript
/**
 * @fileoverview 用户认证服务，处理登录、注册、Token 刷新
 * @module services/auth-service
 */
```

函数注释示例（Dart）：

```dart
/// 解析用户语音文本，提取金额、分类、日期
///
/// [text] 用户输入的语音转文字内容，如 "中午吃面花了25"
/// 返回 [TransactionParseResult] 包含金额、分类、备注
```

### 3.2 API 文档

- 每个接口需在代码中提供 JSDoc 描述 + 请求/响应示例
- 项目根目录维护 `API.md`，跟随代码同步更新

## 四、Git 提交规范

所有 Commit Message 必须遵循 Conventional Commits 格式：

| 类型 | 说明 | 示例 |
|---|---|---|
| feat | 新增功能 | `feat: 首页增加语音记账入口` |
| fix | 修复 Bug | `fix: 修复滑动确认卡片状态丢失问题` |
| docs | 文档更新 | `docs: 更新 API 接口文档` |
| style | 代码格式（不影响逻辑） | `style: 统一缩进为2空格` |
| refactor | 重构（不改变功能） | `refactor: 拆分记账服务为独立模块` |
| test | 测试相关 | `test: 增加账目解析单元测试` |
| chore | 构建/工具链更新 | `chore: 升级 Flutter SDK 至 3.19.0` |

## 五、目录结构规范

### 5.1 后端目录结构（强制）

```
backend/
├── src/
│   ├── index.ts                 # 入口文件
│   ├── config/                  # 配置文件（数据库、环境变量）
│   │   └── database.ts
│   ├── controllers/             # 控制器（处理请求/响应）
│   │   └── transaction.controller.ts
│   ├── services/                # 业务逻辑层
│   │   └── transaction.service.ts
│   ├── repositories/            # 数据访问层（与数据库交互）
│   │   └── transaction.repository.ts
│   ├── routes/                  # 路由定义
│   │   └── v1/
│   │       └── transaction.routes.ts
│   ├── middlewares/             # 中间件（认证、日志、错误处理）
│   │   ├── auth.middleware.ts
│   │   └── error.middleware.ts
│   ├── schemas/                 # 数据校验 Schema（Zod）
│   │   └── transaction.schema.ts
│   ├── types/                   # TypeScript 类型定义
│   │   └── transaction.types.ts
│   └── utils/                   # 工具函数
│       └── logger.ts
├── tests/                       # 单元测试
├── .env.example                 # 环境变量示例
├── package.json
├── tsconfig.json
└── README.md
```

### 5.2 前端目录结构（强制）

```
mobile/lib/
├── main.dart                    # 入口文件
├── config/                      # 配置文件（主题、路由）
│   ├── theme.dart
│   └── routes.dart
├── models/                      # 数据模型（与后端对应）
│   ├── transaction.model.dart
│   └── user.model.dart
├── providers/                   # Riverpod 状态管理
│   ├── transaction.provider.dart
│   └── auth.provider.dart
├── services/                    # 业务服务（API调用、本地DB）
│   ├── api.service.dart
│   └── local_db.service.dart
├── screens/                     # 页面
│   ├── home_screen.dart
│   ├── target_screen.dart
│   └── profile_screen.dart
├── widgets/                     # 可复用组件
│   ├── bottom_nav_bar.dart
│   └── transaction_card.dart
└── utils/                       # 工具函数
    ├── validators.dart
    └── formatters.dart
```

## 六、性能标准

| 指标 | 目标值 | 测量方式 |
|---|---|---|
| 冷启动时间（Flutter） | < 3 秒 | `flutter run --trace` |
| 语音→记账全流程 | < 3 秒 | 手动计时 |
| API 响应时间（P95） | < 500ms | 日志记录 |
| 内存占用 | < 80MB | Android Profiler |
| 数据库查询（本地） | < 50ms | 日志记录 |
| 导出 CSV（1000条） | < 2 秒 | 手动计时 |

## 七、安全规范

| 约束 | 说明 |
|---|---|
| 禁止硬编码密钥 | 所有密钥必须通过环境变量注入 |
| 禁止在日志中打印敏感信息 | 包括密码、手机号、Token |
| 所有用户 ID 必须使用 UUID | 禁止自增 ID 暴露，防止数据遍历 |
| 导出数据需用户主动触发 | 禁止后台自动上传，保持透明 |
| 本地数据库不存储任何用户身份信息明文 | 仅存储加密后的手机号哈希 |

## 八、交付物标准

每个任务完成后，必须提供：

- 可运行的代码（通过编译/无报错）
- 单元测试（核心函数覆盖率 > 60%）
- 代码注释（符合第三节规范）
- 验收记录（截图或日志证明功能正常）

## 九、禁止事项（红线）

| 序号 | 禁止行为 | 后果 |
|---|---|---|
| 1 | 提交未经过本地测试的代码 | 任务驳回，重新提交 |
| 2 | 未经评估擅自引入第三方库 | 需提供选型评估报告 |
| 3 | 使用 `any` 类型逃避类型检查 | 代码审查不通过 |
| 4 | 硬编码环境相关配置 | 代码审查不通过 |
| 5 | 在未与负责人沟通的情况下大规模重构已有代码 | 需先说明原因和方案 |

## 十、任务接收与交付流程

```
┌─────────────────────────────────────────────────────────────┐
│                   任务生命周期                              │
├─────────────────────────────────────────────────────────────┤
│  Step 1: 接收任务 → 确认理解需求，有疑问立即澄清           │
│  Step 2: 本地开发 → 编码 + 自测 + 注释                    │
│  Step 3: 自检验收 → 对照《任务验收标准》逐项确认            │
│  Step 4: 交付报告 → 提交代码 + 输出《交付确认单》           │
│  Step 5: 等待审核 → 负责人验收通过后，接下一个任务          │
└─────────────────────────────────────────────────────────────┘
```
