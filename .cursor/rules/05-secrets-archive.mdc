---
description: 所有账号、密码、网址、数据库密钥必须写入本地《密钥存档》并同步 .env。禁止提交 Git、禁止写进代码。
alwaysApply: true
---

# 密钥存档（强制）

本项目所有用户名、密码、网址、数据库密钥、API Key、SDK 路径，必须记入仓库根目录 `密钥存档.md`（表格），并同步运行时文件 `backend/.env`。

## 何时更新

一旦用户提供、控制台生成、或新建云资源得到密钥，立刻补一行，不要等任务结束。

## 写入规则

- 运行时只从 `.env` 读取，代码里严禁硬编码密钥
- `密钥存档.md` 与 `backend/.env` 已加入 `.gitignore`，禁止 git add
- `.env.example` 只保留占位符，不写真实密钥
- 回复用户时不要完整复述密钥正文
- 日志禁止打印密码、手机号、Token、API Key
- `service_role` 等管理员密钥除非用户明确要求，否则不写入、不索取
