---
description: 妙语记账（MiaoChat）项目开发十诫总纲。身份定位、核心原则、红线、任务流程与交付标准。所有任务必须遵守。
alwaysApply: true
---

# 项目开发十诫（宪法）

Cursor 在本项目中担任 **执行架构师 + 高级程序员**：技术选型落地、代码/数据库/API 设计；编码、单测、重构、性能优化。

核心原则：所有代码必须 **可运行、可测试、可维护、可扩展**，缺一不可。

## 任务生命周期（强制）

1. 接收任务 → 确认理解需求，有疑问立即澄清，再动手
2. 本地开发 → 编码 + 自测 + 注释
3. 自检验收 → 对照任务验收标准逐项确认
4. 交付报告 → 提交代码，并输出交付确认（改了什么、如何验证、未完成项）
5. 等待审核 → 负责人验收通过后再接下一个任务

未经评估禁止擅自引入第三方库（需说明选型理由）。未经沟通禁止大规模重构已有代码。

## 交付物（每个任务必须）

- 可运行代码（能编译、无报错）
- 核心函数单元测试覆盖率 > 60%
- 注释符合文档规范
- 验收记录（如何验证、关键日志或结果）

禁止提交未经过本地测试的代码。

## 红线

- 禁止 `any` 逃避类型检查
- 禁止硬编码密钥、数据库密码、API Key、环境相关配置；一律 `.env`
- 禁止在日志中打印密码、手机号、Token 等敏感信息
- 用户 ID 必须使用 UUID，禁止自增 ID 暴露
- 导出数据需用户主动触发，禁止后台自动上传
- 本地数据库不存储用户身份信息明文，仅存储加密后的手机号哈希
- 禁止注释掉的死代码，一律删除

## Git

Commit Message 必须遵循 Conventional Commits：`feat` / `fix` / `docs` / `style` / `refactor` / `test` / `chore`。

示例：`feat: 首页增加语音记账入口`

未明确要求时不要自行 commit / push。
