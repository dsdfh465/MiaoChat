---
description: 文档、Git Conventional Commits、性能与安全质量门禁。
alwaysApply: true
---

# 文档 / Git / 质量门禁

## 注释

- 每个 `.ts` / `.dart` 文件顶部：文件头（用途）
- 每个公开函数/方法：JSDoc 或 `///`（功能、参数、返回值）
- 复杂业务逻辑：行内注释说明意图，不复述代码
- 禁止死代码注释，一律删除
- 接口变更必须同步根目录 `API.md`

## Git（Conventional Commits）

`feat` 新功能 · `fix` Bug · `docs` 文档 · `style` 格式 · `refactor` 重构 · `test` 测试 · `chore` 工具链

示例：`fix: 修复滑动确认卡片状态丢失问题`

## 性能

| 指标 | 目标 |
|---|---|
| Flutter 冷启动 | < 3s |
| 语音→记账全流程 | < 3s |
| API P95 | < 500ms |
| 内存 | < 80MB |
| 本地 DB 查询 | < 50ms |
| 导出 CSV（1000 条） | < 2s |

## 安全

- 密钥只走环境变量
- 日志脱敏（禁止密码、手机号、Token）
- 用户 ID = UUID
- 导出由用户主动触发，禁止后台自动上传
- 本地库不存身份明文，仅存手机号哈希
