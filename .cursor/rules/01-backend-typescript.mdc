---
description: 后端 TypeScript / Node.js 硬性规范：ESM、类型、Zod 校验、错误处理、分层目录。
globs: backend/**/*.{ts,js}
alwaysApply: false
---

# 后端 TypeScript / Node.js

- 使用 ES Module（`import` / `export`），禁止 `require` / `module.exports`
- 所有函数必须声明参数类型和返回值类型，禁止 `any`
- 所有 API 请求参数必须用 Zod 校验（`backend/src/schemas/`）
- 错误处理必须 `try-catch`，异常记录日志（禁止打印密码、手机号、Token）
- 密钥与环境配置只从 `.env` 读取，提供 `.env.example`，严禁硬编码
- 缩进 2 个空格
- 文件顶部必须有 `@fileoverview` 文件头；公开函数必须有 JSDoc（功能、参数、返回值）
- 每个接口在代码中提供 JSDoc + 请求/响应示例；同步更新根目录 `API.md`

## 分层（强制，禁止跨层乱放）

```
backend/src/
  index.ts
  config/          # 数据库、环境变量
  controllers/     # 请求/响应
  services/        # 业务逻辑
  repositories/    # 数据访问
  routes/v1/       # 路由
  middlewares/     # 认证、日志、错误处理
  schemas/         # Zod
  types/
  utils/
backend/tests/
```

分层文件命名：`{entity}.{layer}.ts`，例如 `transaction.service.ts`。

```typescript
/**
 * @fileoverview 用户认证服务，处理登录、注册、Token 刷新
 * @module services/auth-service
 */

/** 按 ID 查询用户，不存在时抛出 NotFoundError */
export async function getUserById(id: string): Promise<User> {
  try {
    return await userRepository.findById(id);
  } catch (error) {
    logger.error('Failed to get user', { userId: id, error });
    throw error;
  }
}
```
